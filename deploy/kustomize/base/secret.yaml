# Replace with a sealed secret / external secret in real clusters.
# kubectl create secret generic efelant-postgres --from-literal=password=...
apiVersion: v1
kind: Secret
metadata:
  name: efelant-postgres
type: Opaque
stringData:
  password: change-me
  app-password: change-me-app
