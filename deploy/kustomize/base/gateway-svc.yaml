apiVersion: v1
kind: Service
metadata:
  name: efelant-gateway
spec:
  ports:
    - name: ws
      port: 5433
      targetPort: ws
  selector:
    app.kubernetes.io/name: efelant
    app.kubernetes.io/component: gateway
